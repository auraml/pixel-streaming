import { StateProps } from './initial';
declare const KEYS: {
    UPDATE: string;
    UPDATE_PS_CONFIG_KEY: string;
};
declare function Reducer(state: StateProps, action: any): any;
export { Reducer, KEYS };
